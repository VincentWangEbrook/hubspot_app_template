{
  "name": "hubspot-app-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:backend": "pnpm --filter hubspot-backend dev",
    "dev:frontend": "pnpm --filter hubspot-frontend dev",
    "dev": "concurrently \"pnpm dev:backend\" \"pnpm dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}